var fs = require('fs');
var commands = require('./commands');
var pwdCommand = commands['pwd'];
// pwdCommand();
var lsCommand = commands['ls'];
// lsCommand();
var which = commands['which'];
which();
